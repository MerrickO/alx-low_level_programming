#include <stdio.h>
#include <string.h>

/**
 * main - entrypoint
 *
 * description - prints out specific string
 *
 * Return always 0
 */

int main(void)
{
	char str[] = "\"Programming is like buliding a multilingual puzzle";

	puts(str);
	return (0);
}
